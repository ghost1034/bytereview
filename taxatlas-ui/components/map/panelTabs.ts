/** Tabs of the jurisdiction slide-over; mirrored in the URL as ?tab= (rates is the default and omitted). */
export type PanelTab = "rates" | "regulations" | "courts" | "tariffs" | "changes" | "children";
export const PANEL_TABS: PanelTab[] = ["rates", "regulations", "courts", "tariffs", "changes", "children"];
