"""Shared backend constants.

Keep values here when they are used across multiple modules/routes.
"""

# Per-file limit for direct user uploads (Jobs/CPE upload UI).
MAX_DIRECT_UPLOAD_BYTES = 50 * 1024 * 1024
