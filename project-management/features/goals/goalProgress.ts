/** Re-export — backward compat for portfolio stats and legacy imports. */
export {
  computeGoalProgress,
  formatGoalStatus,
  getGoalProgressPercent,
  getGoalStatusColor,
  getMetricProgressPercent,
  inferStatusFromPercent,
  useGoalProgress,
} from '../../lib/goals/goalProgress'
