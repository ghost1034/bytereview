/** Default weekly capacity when User.capacityHoursPerWeek is unset. */
export const DEFAULT_CAPACITY_HOURS_PER_WEEK = 40

/** Assumed workdays per week for daily capacity math. */
export const WORKDAYS_PER_WEEK = 5

/** Default effort hours for a single-day incomplete task. */
export const DEFAULT_SINGLE_DAY_EFFORT_HOURS = 1

/** Default effort hours for a multi-day incomplete task. */
export const DEFAULT_RANGE_EFFORT_HOURS = 4

/** Sentinel id for tasks without an assignee. */
export const UNASSIGNED_USER_ID = '__unassigned__'
